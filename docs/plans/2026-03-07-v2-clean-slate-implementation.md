# V2 Clean Slate Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Preserve the current site under `legacy/` and establish a clean Vite-based single-page app baseline for v2.

**Architecture:** The current PHP site and archive are moved unchanged into a dedicated legacy folder. A new root-level Vite app becomes the active project surface, using plain HTML, CSS, and JavaScript to keep the art piece lightweight and composition-first.

**Tech Stack:** Git, Vite, Vanilla JavaScript, HTML, CSS

---

### Task 1: Preserve the Current Site as Legacy

**Files:**
- Create: `legacy/`
- Move: `_archive/`
- Move: `public_html/`

**Step 1: Inspect the current root layout**

Run: `Get-ChildItem 'C:\matt\WEBSITES\matthewemery.com.au' -Force`
Expected: root contains `_archive`, `public_html`, `.git`, `AGENTS.md`, and `docs`.

**Step 2: Create the legacy container**

Run: `New-Item -ItemType Directory -Force 'C:\matt\WEBSITES\matthewemery.com.au\legacy'`
Expected: `legacy/` exists.

**Step 3: Move the legacy site folders**

Run: `Move-Item 'C:\matt\WEBSITES\matthewemery.com.au\_archive' 'C:\matt\WEBSITES\matthewemery.com.au\legacy\_archive'; Move-Item 'C:\matt\WEBSITES\matthewemery.com.au\public_html' 'C:\matt\WEBSITES\matthewemery.com.au\legacy\public_html'`
Expected: root no longer contains `_archive` or `public_html`; both exist under `legacy/`.

**Step 4: Verify the legacy snapshot**

Run: `Get-ChildItem 'C:\matt\WEBSITES\matthewemery.com.au\legacy' -Recurse`
Expected: original files are present, including `legacy/public_html/index.php` and the image assets.

**Step 5: Commit**

```bash
git add -A
git commit -m "chore: preserve v1 site under legacy"
```

### Task 2: Bootstrap the V2 App Shell

**Files:**
- Create: `package.json`
- Create: `vite.config.js`
- Create: `index.html`
- Create: `src/main.js`
- Create: `src/styles.css`
- Create: `public/`

**Step 1: Initialize the minimal Vite package**

Create `package.json` with scripts for `dev`, `build`, and `preview`, and add `vite` as a dev dependency.

**Step 2: Add the root HTML shell**

Create `index.html` that mounts a single app root and loads `src/main.js`.

**Step 3: Create the first-screen composition scaffold**

Create `src/main.js` and `src/styles.css` with a single full-screen composition that preserves the spare tone of v1.

**Step 4: Add Vite config only if needed**

Create `vite.config.js` only if deployment path handling or static asset rules require it; otherwise keep Vite default-simple.

**Step 5: Install dependencies and verify dev startup**

Run: `npm install`
Expected: `node_modules/` installed with no fatal errors.

Run: `npm run dev`
Expected: local Vite dev server starts successfully.

**Step 6: Commit**

```bash
git add package.json index.html src public vite.config.js
git commit -m "feat: scaffold v2 single-page app"
```

### Task 3: Add Basic Build Verification

**Files:**
- Modify: `package.json`
- Verify: `dist/` generated by Vite build

**Step 1: Run the production build**

Run: `npm run build`
Expected: Vite outputs a successful production build into `dist/`.

**Step 2: Inspect the build output**

Run: `Get-ChildItem 'C:\matt\WEBSITES\matthewemery.com.au\dist' -Recurse`
Expected: built HTML, JS, and CSS assets exist.

**Step 3: Commit if any build-related adjustment was needed**

```bash
git add package.json vite.config.js
 git commit -m "chore: finalize v2 build baseline"
```

### Task 4: Document the New Project Layout

**Files:**
- Modify: `AGENTS.md`

**Step 1: Update contributor guidance**

Adjust `AGENTS.md` so it describes the new root-level Vite app and the read-only `legacy/` snapshot.

**Step 2: Verify the docs still match the repo**

Run: `Get-Content 'C:\matt\WEBSITES\matthewemery.com.au\AGENTS.md'`
Expected: contributor instructions match the actual layout and commands.

**Step 3: Commit**

```bash
git add AGENTS.md
git commit -m "docs: update contributor guide for v2 layout"
```

### Task 5: Final Verification

**Files:**
- Verify: repository working tree

**Step 1: Check Git status**

Run: `git -C 'C:\matt\WEBSITES\matthewemery.com.au' status --short`
Expected: no unexpected uncommitted changes.

**Step 2: Re-run the build**

Run: `npm run build`
Expected: successful build after all layout and documentation changes.

**Step 3: Sanity-check the legacy snapshot remains untouched**

Run: `Get-ChildItem 'C:\matt\WEBSITES\matthewemery.com.au\legacy\public_html'`
Expected: original legacy site files are still intact.
